{
  "name": "test-complier",
  "version": "1.0.0",
  "description": "",
  "main": "app.test.js",
  "scripts": {
    "test": "env NODE_ENV=development nodemon app.test",
    "dev": "env NODE_ENV=development nodemon",
    "server": "node app.test",
    "start": "env NODE_ENV=production npm run server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.6"
  },
  "nodemonConfig": {
    "exec": "clear && npm run server",
    "ignore": "tests/views/**"
  }
}
